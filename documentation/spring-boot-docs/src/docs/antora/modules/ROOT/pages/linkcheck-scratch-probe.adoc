// SCRATCH PROBE — temporary file to prove {attribute} resolution works.
// NOT for upstream. Delete before any real PR.
//
// Uses the exact attribute form from io/grpc.adoc. With linkcheck-attributes.properties
// generated in CI, these must resolve and be live-checked (expect HTTP 404,
// the reported production breakage). Without attributes they warn as unresolved.

Attribute-form broken link: {url-spring-grpc-docs}/server.html#_declarative_security_with_spring_security[Spring gRPC security docs].

Attribute-form second link: {url-spring-grpc-docs}/server.html#_create_a_grpc_service[Spring gRPC service docs].

Healthy absolute link, still checked: https://grpc.io/docs/[gRPC docs].
